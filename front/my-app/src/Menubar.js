function Menubar(){
    const width = window.innerWidth / 20;
    const height = window.innerHegiht / 50;

    return (
        <div>
            <img src = '/Images/하누리.png' alt = '로고' width = {width} height = {height} />
        </div>
    )
}

export default Menubar;